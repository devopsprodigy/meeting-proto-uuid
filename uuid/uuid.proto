syntax = "proto3";

package meeting.uuid.proto;

message Request {}

message Response {
    string uuid = 1;
    string instance = 2;
}

service GetService {
    rpc Get(Request) returns (Response) {}
}
